# General
.idea/
.agents
.cursor
*.log

# Claude Code: .claude/skills and .claude/settings.json ARE tracked so every
# machine and every session gets the same skills. Only the per-machine files
# are ignored -- settings.local.json holds one developer's permission grants
# and would conflict on every pull.
.claude/settings.local.json

# Backend
backend/.venv/
*.egg-info/
__pycache__/
*.pyc
.pytest_cache/
.coverage
backend/.env
backend/app/builders/temp/

# Frontend
frontend/node_modules/
frontend/.next/
frontend/.env.local
frontend/tsconfig.tsbuildinfo

# Supabase
supabase/.temp/